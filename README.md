# k8s-vanilla


```sh
# in the gitops style pure YAML
./run gitops

# in the tf style pure Tofu
./run iac

# setup
./run setup

# bootstrap
./run bootstrap

# cleanup
./run cleanup

# upgrade cluster
./run upgrade

# update hosts
./run update

```